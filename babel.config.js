module.exports = {
  presets: [
    '@babel/preset-env', // transpile code based on the env it needs to be reproduce in, e.g. browser, node.
    '@babel/preset-react' // adds React functionalities to the code in a manner the browser understands.
  ],
};